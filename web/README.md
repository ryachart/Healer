# Healer web rules engine

This workspace contains the Phase 2 browser-port rules-engine slices: deterministic encounter bootstrap state plus combat-runtime spell timing/cooldown, player-healing resolution, and positive spell-effect lifecycle state assembled from the canonical `web/data/*.json` payloads.

## Commands

- `npm run build`
- `npm test`
